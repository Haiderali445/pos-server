/**
 * Invoice Model (Enterprise Alias for Bill)
 * Provides modern ERP naming semantics while maintaining 100% backward compatibility with legacy Bill references.
 */
const Bill = require("./Bill");

module.exports = Bill;
